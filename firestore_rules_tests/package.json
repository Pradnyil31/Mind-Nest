{
  "name": "mindnest-firestore-rules-tests",
  "private": true,
  "version": "1.0.0",
  "description": "Security rules tests for MindNest Firestore access control",
  "scripts": {
    "test": "cross-env XDG_CONFIG_HOME=./.config npx firebase emulators:exec --only firestore \"jest --runInBand --verbose\"",
    "test:jest": "jest --runInBand --verbose"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^3.0.2",
    "firebase": "^10.14.1",
    "firebase-tools": "^14.24.0",
    "jest": "^29.7.0"
  }
}
