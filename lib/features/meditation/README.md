# Meditation Feature

This module will contain meditation-related functionality:

- Meditation and breathing screens/widgets (`presentation/`).
- Controllers and providers for meditation state (`application/`).
- Domain models and logic for sessions, timers, and content (`domain/`).
- Firestore and other integrations (`infrastructure/`).

Like other features, this starts as a scaffold to support gradual, safe refactoring.

