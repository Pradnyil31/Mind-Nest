# Journal Feature

This module will host all journaling-related code:

- Journal screens and widgets (`presentation/`).
- State management, controllers, and Riverpod providers (`application/`).
- Journal domain models and business rules (`domain/`).
- Firestore access and persistence logic (`infrastructure/`).

It starts as a scaffold; existing journaling code will be moved here gradually to avoid large breaking changes.

